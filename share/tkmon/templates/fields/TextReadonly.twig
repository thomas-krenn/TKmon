<fieldset>
    <label for="{{ name }}">{{ label }}</label>
    <input type="text" readonly="readonly" name="{{ name }}" id="{{ name }}" value="{{ value }}" />
    {% if description %}
        <p>
            <i>{{ description }}</i>
        </p>
    {% endif %}
</fieldset>